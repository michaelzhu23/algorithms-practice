function largestNumber(n) {
  // set number 9 to a variable
  // convert variable to string
  // repeat string n times
  // convert back to number
  const number = 9
  return parseInt(number.toString().repeat(n));
}
